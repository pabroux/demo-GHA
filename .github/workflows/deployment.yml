name: Deploy Project
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v4
      - name: Listing code
        run: |
          ls
  deploy:            
    runs-on: ubuntu-latest
    steps:
      - name: Print Deploy
        run: |
            echo "Deploying..."
